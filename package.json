{
  "name": "@0y0/jest-allure-reporter",
  "version": "1.0.6",
  "description": "A custom Jest reporter to compatible with Allure.",
  "main": "lib/index.js",
  "repository": "https://github.com/o0y0o/jest-allure-reporter",
  "author": "Jason Chung <shiningjason1989@gmail.com>",
  "license": "MIT",
  "files": [
    "/lib"
  ],
  "scripts": {
    "pkg": "[ $(yarn info $npm_package_name version) != $npm_package_version ] && yarn publish || echo Skip publishing due to v$npm_package_version exist"
  },
  "dependencies": {
    "fs-extra": "^9.1.0",
    "param-case": "^3.0.4"
  },
  "devDependencies": {
    "prettier": "^2.2.1"
  }
}
